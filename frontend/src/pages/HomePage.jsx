import { PageShell } from '../components/ui/PageShell';

export function HomePage() {
  return (
    <PageShell
      title="Home"
      description="Public landing page shell. Next steps will add hero search, categories, and recommendation blocks."
    />
  );
}
